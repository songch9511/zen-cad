interface:
  display_name: "CAD Spec"
  short_description: "Spec CAD, checks, and specialist work."
  default_prompt: "Use $cad-spec to turn this mechanical request into a CAD-native layout spec with coordinate frames, parameters, artifact targets, interface primitives, inspection checks, visual inspection, engineering review, repair rules, locked facts, a Subagent Dispatch Plan with fallback behavior, specialist subagent tasks, a proceed gate, and a downstream CAD handoff."
