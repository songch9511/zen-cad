# Release Review Prompt

Prepare a Zen CAD release only after required files, schemas, reference milestones, and documentation pass validation. Confirm the release preserves `/cad-spec` as the default source of truth, keeps `/agentic-cad` only as a compatibility shim, and does not include private machine-specific paths.
